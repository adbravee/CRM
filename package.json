{
  "name": "support-crm",
  "private": true,
  "version": "1.0.0",
  "description": "Support CRM — single deploy (Express + SQLite + React)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && cross-env NODE_ENV=production npm run start --prefix backend",
    "start:server": "cross-env NODE_ENV=production npm run start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
